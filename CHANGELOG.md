# 更新日志

## 0.1.65

- 修复 macOS 发布包只有残缺临时签名、被 Gatekeeper 误报为“应用已损坏”的问题。
- 未配置 Apple Developer 证书时，改为对完整应用执行可验证的 ad-hoc 结构签名，并将安装包明确标记为 `UNSIGNED-PREVIEW`，仅用于安装测试。
- 配置 Apple Developer 证书后，发布流程会强制验证 Developer ID、Team ID、严格签名、Gatekeeper 接受结果和苹果公证票据。

## 0.1.64

- 删除“统一响度”开关及自动响度增益处理；原声和 BGM 只按各自音量滑杆的比例混合。
- 新任务会忽略旧客户端或旧项目残留的统一响度字段，确保本地与服务器都不会重新启用自动增益。
- 已经开始或暂停的旧服务器批次继续保留原音频管线，避免同一批成片在服务器升级后中途改变声音规则。
- 服务器音频管线升级到 v10，桌面端会拦截仍可能启用统一响度的旧服务器。

## 0.1.63

- 修复关闭“统一响度”后，最终限幅器仍按 FFmpeg 默认设置自动补偿增益、使 BGM 和整体混音被额外抬高约 0.45 dB 的问题。
- 最终限幅器改为只压制可能削波的峰值，不再主动放大音频；BGM 100% 时保持素材原始增益，其他百分比严格按滑杆比例处理。
- 每个服务器任务保存自己的音频算法版本；服务器升级后，已暂停或待恢复的旧批次继续使用原规则，新批次才使用修复后的规则，避免同一批成片前后响度不同。
- 服务器音频管线升级到 v9，桌面端会拦截仍带有自动增益的旧服务器。

## 0.1.62

- 为每个混剪任务保存组合算法版本；服务器升级或重启后，进行中和排队中的旧任务继续按原顺序恢复，不会在同一批次中途切换排列规则。
- 0.1.60 及更早桌面端提交的任务保持严格组合顺序；0.1.61 及以上新任务使用均匀轮换，兼容多人持续使用服务器时的平滑升级。

## 0.1.61

- 修复最大成片数量小于全部排列组合时，后段和结尾素材长期固定为第一个、其余候选素材没有参与混剪的问题。
- 各段素材改为从第一批成片开始均匀错位轮换；例如生成 100 条且有 3 个结尾时，三个结尾分别使用 34、33、33 次。
- 保留完整笛卡尔积且不生成重复的完整组合；相同项目的组合顺序稳定，便于失败重试和定位成片来源。
- 服务器组合管线升级到 v4，桌面端会拦截仍使用旧组合顺序的服务器。

## 0.1.60

- 修复关闭“统一响度”后加入 BGM 会按音轨数量自动压低人声的问题；原声 100% 现在保持用户设定的增益，不再因 1 条 BGM 降约 6 dB、因 2 条 BGM 降约 9.5 dB。
- 混音改为按原声和 BGM 滑杆的实际比例叠加，并在最终输出阶段限制削波；BGM 不会再因为人声被自动压低而显得异常突出。
- 服务器音频管线升级到 v8，桌面端会阻止旧服务器继续产生错误音量的成片。

## 0.1.59

- 服务器成片回传前会检查本地输出磁盘空间，并按待下载文件大小预留安全余量；空间不足时会显示剩余空间、所需空间和输出目录，不再笼统误报为服务器下载失败。
- 服务器成片下载失败后可直接点击“继续下载”；已经完整写入本地的成片会按服务器文件大小校验并跳过，只补下载缺失文件，不会重新混剪。
- 本地磁盘写满时立即停止无效重试，并明确提示清理空间或更换输出磁盘，避免同一文件连续失败三次。

## 0.1.58

- 修复侯秋艺批次的 PCM `ipcm` 音轨被误判为不可解码、最终生成静音 AAC 成片的问题；已用该批真实素材验证修复后平均音量为 -8.6 dB。
- 桌面端内置 FFmpeg/FFprobe 从旧版升级到 7.1，兼容更多手机、相机和剪辑软件视频编码；扩展 TS、MTS、M2TS、MPG、VOB、3GP、FLV、WMV、MXF、RMVB 等常见输入格式。
- 没有可解码原声且没有有效 BGM 时直接指出问题素材，不再生成静音文件并标记成功。
- 服务器音频管线升级到 v7，桌面端会拦截旧服务器，避免本地已修复而服务器仍产出静音成片。

## 0.1.57

- 修复关闭主窗口后清理任务实例时继续读取已销毁 `webContents`，导致主进程弹出 `TypeError: Object has been destroyed` 的问题。
- Windows 与 macOS 发布流程新增主窗口关闭回归，确保窗口销毁时不会再触发 JavaScript 主进程异常。

## 0.1.56

- 云管家登录区新增“退出当前账号”，会清除本机保存的账号身份、网页上传授权和 OpenAPI 临时令牌。
- 退出状态会同步到所有混剪任务标签，并清空旧账号留在界面中的云素材查询结果，避免其他标签把旧账号重新保存回来。

## 0.1.55

- 服务器任务 ID、项目配置、队列顺序和最新进度改为实时落盘；混剪服务异常退出或被外部维护任务重启后，会自动恢复原任务。
- 恢复任务会扫描已经通过校验并原子发布的成片，跳过已完成组合，从缺失的下一条继续，不会覆盖或重复编码已有结果。
- 自动清理崩溃时遗留的 `.partial` 临时成片，避免无效文件长期占用服务器磁盘。
- 已完成任务在服务器重启后仍保留查询和成片下载能力，避免混剪刚完成但尚未回传本机时出现“任务不存在”。
- 服务收到停止信号时会先刷新任务队列和看板状态，缩小异常关停时的进度丢失窗口。

## 0.1.54

- 服务器的两个并发槽位现在强制绑定不同项目；同一项目重复提交时会排队，直到该项目已有批次结束。
- 服务器拒绝跨项目读取素材、BGM、剪映模板或写入成片，防止多个剪辑在同一时间混剪时互相串素材、覆盖输出。
- 排队任务实时显示当前排队位次、总排队数和服务器并发槽位数。
- 服务器支持通过 `MIX_FFMPEG_THREADS` 限制每个 FFmpeg 编码任务的线程数，避免两个项目同时混剪时占满整机 CPU。

## 0.1.53

- 修复同路径素材被替换后仍沿用旧时长、旧尺寸和旧响度缓存的问题，导出额外校验预期总时长。
- 修复关闭统一响度或主动调低音量后，成片被自动增益重新放大的问题；响度处理在用户音量调节之前完成。
- 保护已有成片：同目录同名文件会明确提示更换目录或名称，不再直接覆盖；编码和回传使用临时文件，校验通过后发布，并防止并发任务覆盖彼此的输出。
- 修复本地任务在素材下载、探测、响度检测和编码阶段无法及时停止的问题；取消后清理未完成文件。
- 修复服务器素材上传失败后任务仍卡在运行中、上传期间停止仍创建任务，以及停止后不等待服务器确认的问题；支持取消回传并增加传输无进展超时。
- 修复云管家上传记录异步读取会自行取消、已提交状态不能恢复和自动发布卡住的问题；失败重试后重新恢复上传记录。
- 混剪运行中锁定素材及导出设置，启动时保存本批输出清单，防止编辑下一批导致发布清单错位。
- 修复带旋转信息的竖拍素材在“原来尺寸”模式下误用横向画布的问题。
- 服务器音频管线升级为 v6，需要同步更新服务器；发布流程新增界面回归，Windows 与 macOS 均验证成功后才发布。

## 0.1.52

- 修复素材音轨比画面长时，上一段末帧被音频拖长而出现卡住的问题；每个段落现在严格以视频流时长为准切换。
- 修复素材音轨短于画面时的尾部静音和音轨缺失问题；原声会自动补齐或裁剪到对应画面长度，再参与整片拼接和 BGM 混音。
- 成片导出后新增音轨存在、静音和音画时长一致性校验；不合格组合会明确失败，不再把无声或不同步文件当作成功。
- 服务器音频管线升级到 v5，旧服务器会被桌面端阻止启动；发布前完整链路测试新增长短音轨、高帧率 PCM 相机素材及 24 条服务器并发回传校验。

## 0.1.51

- 云管家上传改为可恢复任务：每个混剪任务会在输出目录保存上传清单，记录文件指纹、云端地址、提交状态、requestId 与失败原因，不保存登录 Token。
- 软件关闭、网络中断或升级后再次打开同一任务，会自动识别已上传和已提交成片，只继续待上传、待导入或失败项；已替换的同名文件会按新文件重新处理。
- 自动发布会先恢复上传清单，再决定是否继续，避免刚恢复任务时把已完成成片重复上传或重复提交。

## 0.1.50

- 修复大量成片自动上传云管家时，单个文件或单批请求报错会中断所有后续批次的问题；现在按每批 50 条连续处理，个别文件失败不会阻塞其余视频。
- 本地直传增加单文件重试、连接无响应超时保护及失败记录；已成功上传的 URL 会立即保留，重新点击发布只补传未完成项，不会重复导入已成功的视频。
- 修复混剪完成后的自动发布列表被重复初始化而丢失已上传 URL 的问题；发布表新增每条视频的待上传、待导入、已提交或待重试状态。

## 0.1.49

- 每个浏览器式任务标签的服务器地址与 Token 改为独立保存；任务 A 修改服务器连接不会覆盖任务 B，旧版本保存的连接信息会仅作为首次迁移读取。
- 每个标签的本地/服务器混剪实例、素材、输出目录、进度、失败重试和云管家本次发布状态均独立；关闭已停止标签会释放对应后台运行实例，避免旧任务事件残留。
- 云管家登录授权与可复用的发布配置模板继续全局共用，避免同一账号重复登录；每个任务当前选择和发布进度仍保持独立。
- 修复正式桌面预加载脚本遗漏服务器“重试失败”接口的问题。

## 0.1.48

- 修复同一任务标签先后切换本地/服务器混剪时，界面可能混入另一种混剪位置的进度与失败日志；现在仅显示当前混剪位置的任务状态，并阻止同一标签同时启动两种混剪。
- “重试失败”会根据当前混剪位置执行：服务器任务在服务器上重试，不会错误启动本地任务。
- 成片音轨首次检测为静音时会短暂复检，避免大文件刚写完时发生误判；实际 FFmpeg 失败也会优先显示真正的错误，而不是堆满 DTS 时间戳警告。
- 服务器音频引擎版本升级到 4，包含 BGM 分段时长保留、音量匹配和可执行 FFprobe 回退修复。

## 0.1.47

- 服务器混剪新增空间保护：开始任务前会检查可用空间，默认保留至少 30GB；已完成且回传本地超过 24 小时的服务器项目会自动清理，进行中和排队任务不会受影响。
- 修复服务器文件传输可靠性：素材上传和成片下载均支持重试、完整性校验和临时文件原子替换，网络中断不会留下可被误用的半截文件。
- 服务器启动混剪前会逐项验证段落素材与 BGM 是否已完整上传，避免把“素材不存在”或“上传不完整”误报成混剪成功。
- 修复服务器背景音乐同步与音频兼容处理，原声关闭时仍会保留可听见的 BGM。
- 发布前已实测本地与正式服务器完整链路：3 个并发/排队任务共 24 条严格排列组合全部混剪、回传完成，并完成 BGM 音频校验。

## 0.1.45

- 混剪组合统一改为严格笛卡尔积：A 的每个素材都会与 B、C、D 等后续段落的每个素材逐一组合，不再使用错位分散、随机抽样或其他特殊排序规则。
- 组合顺序固定为 A 变化最快、随后 B、C、D；例如 A=2、B=2、C=2 时会生成 8 条且无重复，完整覆盖所有组合。
- 服务器混剪同步使用同一组合算法，并新增 A/B/C 三段真实服务器验收，验证组合顺序、成片回传、画面和声音。
- 修复服务器内置 FFmpeg 缺失时整批导出失败的问题：内置二进制不可用时自动回退到服务器系统 FFmpeg/FFprobe。

## 0.1.44

- 修复服务器混剪仍连续使用同一个 A 段开头的问题：服务器已升级为与桌面端一致的开头优先轮换顺序，A 有 2 个素材时成片会按 A1、A2、A1、A2 交替。
- 新增服务器组合引擎版本校验；服务器过旧时会明确提示并阻止启动，避免无提示地生成开头重复的成片。
- 发布前完整验收新增服务器实际成片开头顺序校验，覆盖素材上传、服务器混剪、回传本机、画面、声音与开头轮换。

## 0.1.43

- 云管家发布支持自动分批：超过 50 条时按每批最多 50 条依次上传并提交导入，例如 100 条会自动处理为第 1 批 1-50 条和第 2 批 51-100 条。
- 本地成片直传和公网 URL 导入都使用同一分批逻辑；每批完成后才会继续下一批，避免触发云管家单次导入上限。
- 每批成功上传后立即保存公网地址；后续批次失败或标签失效时，重新点击发布会优先复用已上传成片，不会重复上传。
- “查询结果”会自动汇总本次所有批次的导入状态，不需要手动逐个查询 requestId。

## 0.1.42

- 优化全交叉组合的输出顺序：A、B 段优先交叉轮换，C 及后续段落会随前面组合错位分散，避免只生成前 N 条时后续段落长期固定为同一素材。
- 不改变总组合数、不重复任何组合；完整生成时仍会覆盖每个素材与其他段落素材的全部组合。
- 新增后续段落提前分散和完整组合无重复测试，并完成本地及服务器混剪回传验证。

## 0.1.41

- 调整排列组合顺序：A 段优先轮换，后续段落依次推进；当 A 有 2 个素材、B 有 20 个素材并生成 40 条时，成片开头将按 A1、A2 交替，不再连续使用同一个开头。
- 新增 40 条组合的自动测试，确保后续片段素材数量较多时仍能均匀轮换开头素材。

## 0.1.40

- 新增“任务标题”字段，可填写本次混剪对应的脚本名称；进度看板和飞书提醒统一显示该标题。
- 旧项目没有填写任务标题时，会优先使用项目文件夹名，其次使用成片名称，不再把服务器生成的 `desktop-...` ID 当作任务标题。
- 看板详情保留系统任务 ID，方便排查问题，但不再占用任务卡片主标题位置。

## 0.1.39

- 新增公司内网混剪进度看板，手机和电脑可实时查看素材传输、排队、混剪、下载、云管家上传及云端处理结果。
- 任务历史会持久保存，服务器重启时会标记未完成任务，且不会把 Token、本地路径等敏感信息写入看板记录。
- 支持飞书群自定义机器人 Webhook 和自建应用机器人通知，只有云管家确认成功/部分成功后才发送完成提醒，失败、中断或处理超时仍会及时告警。
- 看板会从混剪软件当前云管家登录信息识别实际上传人，并在任务卡片、历史、详情和飞书通知中显示姓名与登录手机号。
- 看板明确区分混剪中、混剪完成、上传中、上传完后云端处理中、上传成功和上传失败；没有云管家 requestId 的旧记录显示为“未确认上传”。

## 0.1.38

- 发布前新增完整混剪工作链路验收：使用 A=2、B=2 的不同尺寸和帧率素材，真实生成 4 条组合并验证 BGM 轮换、分段淡出、输出文件数量。
- 成片自动校验 H.264 视频、AAC 音频、非静音和非黑屏；任一校验失败即阻止发布。
- 同时验证服务器素材上传、排队混剪、成片回传至本地输出目录；Windows 和 macOS 的 GitHub 打包流程也会自动执行本地完整混剪验收。

## 0.1.37

- 发布云管家前会按当前视频分类重新加载标签，避免旧配置中的标签跨分类沿用。
- 云管家导入若拒绝某个标签 ID，软件会明确显示被拒绝的标签，并从当前选择中移除，提醒重新选择标签后保存配置。
- 已上传成片因标签校验失败时，修复后的“发布”会直接复用已有公网地址重新提交导入，不再重复上传整批视频。

## 0.1.36

- 修复服务器任务“已完成”但本机输出目录为空的问题：只有服务器实际生成并返回 MP4 后，才会显示成片已回传本地。
- 当服务器端组合全部失败时，任务会直接显示失败，并展示第一条实际失败原因，不再把空结果误报为成功。
- 服务器成功完成时会明确显示已下载到本机的成片数量；部分失败仍会保留已成功回传的成片和失败记录。

## 0.1.35

- 服务器混剪区域改为只显示“服务器连接成功”或“服务器连接失败”，不再展示内部兼容提示和冗长状态信息。
- 云管家“视频信息”支持保存多组可命名配置，并可随时选择、覆盖保存或删除。
- 每组配置会保存视频分区、一级/二级分类、标签、发布形式、命名方式、可见性、同步开关和旋转设置；配置保存在本机，不包含登录信息或 Token。

## 0.1.34

- 为尚未升级的服务器加入音频兼容模式：桌面端会保留本机已确认的原声音轨信息，跳过旧服务器会误判音频的重复探测。
- 已用当前服务器实测验证：旧流程成片为 -91 dB 静音，兼容模式下可恢复实际音轨。

## 0.1.33

- 修复服务器混剪成片带有 AAC 音轨但实际静音的问题：服务器端音频探测失败时，保留桌面端已经确认的“有声音”素材信息，不再补入静音轨。
- 成片导出后新增静音校验；本应有原声或 BGM 的成片若检测为静音，会明确报错而不再伪装成成功。
- 桌面端会识别旧版服务器混剪引擎并阻止启动，避免旧服务器继续生成无声视频。

## 0.1.32

- 修复混剪成片仍然听不到声音的问题：导出时按素材实测音量做最低可听增益补偿，并用峰值限幅避免爆音。
- 没有 BGM 时也会走同一条最终音频链路，不再直接映射原始拼接音轨。
- 导出完成后会检测成片音量；如果成片接近静音，会自动只修复音频轨，视频轨保持不重编码。
- 新增极低音量素材的导出测试，避免生成有音轨但音量接近静音的 MP4。

## 0.1.31

- 修复部分素材混剪后成片没有声音的问题。
- 导出时为每段视频原声重置音频时间戳，并统一重采样到 44100Hz 双声道，避免不同相机、不同帧率素材拼接后音频丢失。
- BGM 轨道也会在裁剪和延迟前统一重采样，提高分段 BGM 混音稳定性。

## 0.1.30

- 修复 macOS 发布流程的 GitHub Actions 表达式问题，改为在打包脚本内判断 Apple 证书配置。
- 未配置 Apple Developer ID 时会完全移除空签名变量，避免 electron-builder 误触发签名失败。
- 重新发布 Windows 安装包和 macOS arm64 DMG/ZIP 包。

## 0.1.29

- 修复 macOS GitHub Actions 打包：未配置 Apple Developer ID 证书时不再传入空签名变量，避免 electron-builder 误触发签名失败。
- Release 将重新生成 macOS arm64 DMG/ZIP 包；未签名包仍可能被 Gatekeeper 拦截，正式分发仍需要 Apple Developer ID 签名和公证。
- 保留 0.1.28 的服务器混剪回传本机、本机统一上传云管家的逻辑。

## 0.1.28

- 服务器混剪完成后先把 MP4 成片下载回本机输出目录，再由本机统一上传云管家。
- 桌面端不再调用服务器的云管家上传接口，服务器只负责素材上传、排队混剪、结果下载。
- 修复服务器任务完成后本机文件尚未下载完成就触发云管家上传的抢跑问题。
- 调整发布流程：Windows 安装包构建成功即可发布；macOS 包失败时不再阻断 Windows 更新。

## 0.1.27

- 服务器混剪完成后先把 MP4 成片下载回本机输出目录，再由本机统一上传云管家。
- 桌面端不再调用服务器的云管家上传接口，服务器只负责素材上传、排队混剪、结果下载。
- 修复服务器任务完成后本机文件尚未下载完成就触发云管家上传的抢跑问题。

## 0.1.26

- 修复 GitHub macOS 打包流程：未配置完整 Apple Developer ID 签名和公证凭据时，不再让 macOS 构建阻断 Windows 安装包发布。
- 本版本包含 0.1.25 的服务器云管家上传修复；Windows 用户可直接安装本版。

## 0.1.25

- 修复服务器混剪完成后发布云管家时，失败组合误上传不存在文件导致的“本地视频不存在”报错。
- 桌面端只将成功生成的组合加入发布清单，并明确提示自动跳过的失败组合数量。
- 服务器端只从当前任务实际生成的 `outputs/videos` 中匹配并上传 MP4，不再信任 Windows 或旧服务器传来的绝对路径。
- 补充服务器升级说明；使用服务器混剪发布云管家时，桌面端和服务器都需要升级到本版本代码。

## 0.1.24

- “任务分身”改为软件窗口内的浏览器式任务标签页，可通过顶部加号或“新建任务标签”创建多个项目。
- 每个标签页独立保存项目素材、段落设置、输出目录、混剪位置和本机/服务器任务进度；切换标签不会中断正在执行的任务。
- 任务标签会按排队、运行、暂停、完成和失败显示状态；运行中的任务标签不能直接关闭，避免误中断。

## 0.1.23

- 新增“新建任务分身”：每个分身窗口可独立配置脚本、素材、输出目录和混剪位置，同时执行互不干扰。
- 本机和服务器任务控制器按分身窗口隔离，不再因已有任务运行而阻止第二个项目启动。
- 服务器新增项目级并发调度，默认同时执行 2 个项目；超出部分自动排队，前序任务完成后继续。
- 补充服务器部署与分身并发配置说明。

## 0.1.22

- 新增服务器混剪模式：可把素材上传到服务器执行混剪，缓解本机性能压力。
- 混剪位置和导出目标独立选择，支持本机/服务器混剪分别导出本地、上传云管家或两者都做。
- 服务器端支持本地素材上传、任务轮询、结果下载和云管家直传。
- 更新方式改为手动更新：软件检查 GitHub 最新版本后打开下载页，不再自动下载或自动安装。

## 0.1.21

- 云管家发布标签改为复选标签组，可同时选择多个二级标签。
- 已选标签会显示数量和名称，并支持一键清空；提交时按接口要求传递多个标签 ID。

## 0.1.20

- “下载新版”改为在软件内部下载更新，不再跳转 GitHub 页面。
- 下载完成后按钮变为“重启安装”，由 Electron 自动替换旧版本。
- 下载过程显示进度，更新失败时保留错误状态。

## 0.1.19

- 优化批量混剪速度：支持极速模式，关闭响度统一时减少额外音频分析。
- 修复不同参数、不同帧率和不同尺寸素材混剪时的兼容处理。
- 修复 macOS 开发版启动时本地 Electron 能力未连接的问题。
- 增强 BGM 分段、淡入淡出和多轨道处理。
- 同步发布 Windows 安装包和 macOS 安装包，旧版本可在“检查更新”后手动下载新版。

## 0.1.14

- 关闭启动时自动检查更新，避免软件一打开就触发下载。
- 检查更新只负责提示新版本，不再自动下载或自动安装。
- 更新区域将“重启安装”改为“下载新版”，点击后打开 GitHub 最新发布页手动下载安装。
- 避免未签名 macOS 包自动安装失败导致误显示“版本号获取失败”。

## 0.1.13

- 修复 macOS 检测更新时提示“版本号获取失败”的问题。
- macOS 发布包新增 zip 更新包，供 Electron 自动更新使用。
- DMG 继续保留给用户手动下载安装。

## 0.1.12

- 更新区域新增“更新日志”按钮，可在软件内查看最新版本说明。
- 更新日志弹窗会展示当前版本、最新版本、发布时间和 GitHub Release 正文。
- 弹窗支持刷新日志和打开 GitHub 发布页。
- 发布流程会优先读取本文件中对应版本的更新内容，方便后续版本写清楚改了什么。
